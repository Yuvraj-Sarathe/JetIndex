# `demo/` — 2-minute demo video (Sneh)

Deliverable: `demo/apix_demo.mp4` (≤ 2 min, 1080p). Large videos: upload to Drive/YouTube (unlisted) and put the link in root README; keep only the script here if > 50 MB.

Shot list (`video_script.md` has timings):
0:00 – Problem in one line + dashboard hero shot
0:15 – Trigger sweep (Flower / `POST /admin/trigger-sweep`), show scraper logs fetching a route
0:45 – Raw JSON → `make pipeline` output showing unbundled columns
1:05 – `make index` → APIx value; explain weights in one sentence
1:20 – Dashboard: trend, heatmap, elasticity, unbundling inspector
1:45 – `/api/v1/apix/daily` with Bearer token in Swagger; backtest vs DGCA
1:55 – Close

Record with OBS/Loom; captions on; no audio music needed.
